// drugs sorted by # of side effects
MATCH (d:Drug)-[:CAUSES]->(:SideEffect)
RETURN d.chembl_id AS chembl_id,
       d.name      AS drug_name,
       count(*)    AS sideeffect_count
ORDER BY sideeffect_count DESC
LIMIT 20;
